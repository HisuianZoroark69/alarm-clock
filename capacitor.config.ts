import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alarm-clock-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
